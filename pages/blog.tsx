import Head from 'next/head';

import Layout from '../components/layout';

const Blog = () => (
  <Layout>
    <Head>
      <title>Blog</title>
      <link rel="icon" href="/favicon.ico" />
    </Head>
  </Layout>
);

export default Blog;
